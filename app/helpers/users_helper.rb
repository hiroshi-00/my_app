module UsersHelper
  
  def user_search(search)
    # @users = User.where(gender: search[:gender])
    # @users = @users.where(age: search[:min_age]..search[:max_age])
    # @users = 
  end
  
  
end
